# Git needs this to verify SSH signatures, not just to make them; without it
# %G? reports N for every commit, correctly signed ones included.
#
# The principal is matched against the commit's author email, so it has to be
# the address in [user] of shared/dotfiles/.gitconfig, not the comment attached
# to the key. Only keys listed here verify; everyone else's commits show N.
nei.neto@hotmail.com ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKT3mc2+decBrbSE9siXK1EMH4EWNEPPrWKQ8qx7EXSV
