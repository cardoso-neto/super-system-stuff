# Work-only setup for this laptop. Nothing here belongs on a personal machine,
# which is why it is under hosts/ rather than os/macos/.

brew install --cask nordlayer  # work VPN

# Where work repos live.
mkdir -p ~/quorum
